#include "include/display.h"
#include "include/util.h"

#include <stdio.h>

int main() {
    /*clear_screen();
    char* line;
    for (int i = 1; i <= 35; i++) {
        int_to_string(i, line, 50);
        print_string(line);
        print_nl();
    }*/
    printf("Hello, world!");
    return 0;
}